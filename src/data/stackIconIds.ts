/**
 * IDs soportados por `StackIcon.astro` (proyectos, pills, etc.).
 * Para añadir uno nuevo: amplía este union y el componente.
 */
export type StackIconId =
  | 'astro'
  | 'wordpress'
  | 'rest'
  | 'brevo'
  | 'netlify';
